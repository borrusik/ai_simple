# AI Simple Chatbot

![Status](https://img.shields.io/badge/Status-Development-yellow)
![Language](https://img.shields.io/badge/Language-Python%203.x-blue)
![Library](https://img.shields.io/badge/Library-PyTorch%20%7C%20NLTK-orange)

Простий та гнучкий чат-бот на основі нейронної мережі (Feed Forward Neural Network), написаний на Python з використанням PyTorch. Проект дозволяє навчити бота на власному наборі даних (JSON) для розпізнавання намірів (intents) та відповідей на запитання.

Поточна конфігурація налаштована на **українську мову**.

## 📋 Опис

Проект реалізує повний цикл створення чат-бота: від обробки природної мови (NLP) до навчання моделі та запуску інтерфейсу.

**Основні можливості:**
* **Розпізнавання намірів:** Класифікація тексту користувача на основі заданих патернів.
* **NLP конвеєр:** Токенізація, стемінг (PorterStemmer) та створення "мішка слів" (Bag of Words).
* **Нейромережа:** Модель на PyTorch із прихованими шарами та функцією активації ReLU.
* **Гнучкість:** Усі діалоги зберігаються у файлі `intents.json`, який легко редагувати.
* **Інтерфейс:** Консольний режим та GUI на Tkinter.

## 🛠 Технологічний стек

* **Мова:** Python
* **ML Фреймворк:** PyTorch
* **NLP:** NLTK (Natural Language Toolkit)
* **GUI:** Tkinter
* **Дані:** JSON

## 📂 Структура проекту

* `train.py` — Скрипт для навчання моделі. Завантажує `intents.json`, обробляє дані та зберігає навчену модель у `data.pth`.
* `chat.py` — Основний скрипт для запуску чат-бота в консольному режимі. Завантажує модель та веде діалог.
* `model.py` — Архітектура нейронної мережі (`NeuralNet`).
* `nltk_utils.py` — Допоміжні функції для обробки тексту (токенізація, стемінг).
* `intents.json` — База знань бота (наміри, фрази-тригери, відповіді).
* `app.py` / `ui.py` — Графічний інтерфейс користувача (GUI).

## 🚀 Встановлення та Запуск

### 1. Встановлення залежностей

Переконайтеся, що у вас встановлено Python. Потім встановіть необхідні бібліотеки:

```bash
pip install torch torchvision nltk
```
Також необхідно завантажити пакет токенізації NLTK (зазвичай скрипт робить це автоматично, але можна виконати вручну):

```Python
import nltk
nltk.download('punkt')
```
### 2. Налаштування даних (опціонально)
Откройте файл intents.json и добавьте свои намерения (intents). Структура:

tag: Категория (например, "greeting").

patterns: Что пишет пользователь ("Привіт", "Хай").

responses: Как отвечает бот ("Привіт!", "Доброго дня!").

### 3. Навчання моделі
Перед першим запуском або після зміни intents.json необхідно навчити модель:

```Bash
python train.py
```
Після завершення навчання буде створено файл data.pth.

### 4. Запуск чат-бота
Вариант А: Консольний режим

```Bash
python chat.py
```
Бот відповідатиме прямо в терміналі. Для виходу введіть quit.

Вариант Б: Графічний інтерфейс (GUI)

```Bash
python app.py
```
Запуститься вікно з чатом.

## 🤖 Приклад роботи

Модель класифікує введення користувача та обирає відповідь з найбільшою ймовірністю. Якщо впевненість моделі низька (prob < 0.75), бот відповість "Я не розумію вас".

> **User:** "Привіт"
> **Bot:** "Доброго дня!"
>
> **User:** "Як скласти резюме?"
> **Bot:** "Зазвичай, роботу можна знайти на спеціалізованих сайтах..."

## 👤 Автор

**borrusik**
