# AI Simple Chatbot

![Status](https://img.shields.io/badge/Status-Development-yellow)
![Language](https://img.shields.io/badge/Language-Python%203.x-blue)
![Library](https://img.shields.io/badge/Library-PyTorch%20%7C%20NLTK-orange)

Простой и гибкий чат-бот на основе нейронной сети (Feed Forward Neural Network), написанный на Python с использованием PyTorch. Проект позволяет обучить бота на собственном наборе данных (JSON) для распознавания намерений (intents) и ответов на вопросы.

Текущая конфигурация настроена на **украинский язык**.

## 📋 Описание

Проект реализует полный цикл создания чат-бота: от обработки естественного языка (NLP) до обучения модели и запуска интерфейса.

**Основные возможности:**
* **Распознавание намерений:** Классификация текста пользователя на основе заданных паттернов.
* **NLP конвейер:** Токенизация, стемминг (PorterStemmer) и создание "мешка слов" (Bag of Words).
* **Нейросеть:** Модель на PyTorch с скрытыми слоями и функцией активации ReLU.
* **Гибкость:** Все диалоги хранятся в файле `intents.json`, который легко редактировать.
* **Интерфейс:** Консольный режим и GUI на Tkinter.

## 🛠 Технологический стек

* **Язык:** Python
* **ML Фреймворк:** PyTorch
* **NLP:** NLTK (Natural Language Toolkit)
* **GUI:** Tkinter
* **Данные:** JSON

## 📂 Структура проекта

* `train.py` — Скрипт для обучения модели. Загружает `intents.json`, обрабатывает данные и сохраняет обученную модель в `data.pth`.
* `chat.py` — Основной скрипт для запуска чат-бота в консольном режиме. Загружает модель и ведет диалог.
* `model.py` — Архитектура нейронной сети (`NeuralNet`).
* `nltk_utils.py` — Вспомогательные функции для обработки текста (токенизация, стемминг).
* `intents.json` — База знаний бота (намерения, фразы-триггеры, ответы).
* `app.py` / `ui.py` — Графический интерфейс пользователя (GUI).

## 🚀 Установка и Запуск

### 1. Установка зависимостей

Убедитесь, что у вас установлен Python. Затем установите необходимые библиотеки:

```bash
pip install torch torchvision nltk
```

Также необходимо загрузить пакет токенизации NLTK (обычно скрипт делает это автоматически, но можно выполнить вручную):

```python
import nltk
nltk.download('punkt')
```

Вот README.md для вашего Python-проекта с искусственным интеллектом. Я составил его, ориентируясь на содержимое файлов (использование PyTorch, NLTK, Tkinter и JSON-датасета).

Этот файл описывает процесс обучения модели и запуска чат-бота.

Markdown
# AI Simple Chatbot

![Status](https://img.shields.io/badge/Status-Development-yellow)
![Language](https://img.shields.io/badge/Language-Python%203.x-blue)
![Library](https://img.shields.io/badge/Library-PyTorch%20%7C%20NLTK-orange)

Простой и гибкий чат-бот на основе нейронной сети (Feed Forward Neural Network), написанный на Python с использованием PyTorch. Проект позволяет обучить бота на собственном наборе данных (JSON) для распознавания намерений (intents) и ответов на вопросы.

Текущая конфигурация настроена на **украинский язык**.

## 📋 Описание

Проект реализует полный цикл создания чат-бота: от обработки естественного языка (NLP) до обучения модели и запуска интерфейса.

**Основные возможности:**
* **Распознавание намерений:** Классификация текста пользователя на основе заданных паттернов.
* **NLP конвейер:** Токенизация, стемминг (PorterStemmer) и создание "мешка слов" (Bag of Words).
* **Нейросеть:** Модель на PyTorch с скрытыми слоями и функцией активации ReLU.
* **Гибкость:** Все диалоги хранятся в файле `intents.json`, который легко редактировать.
* **Интерфейс:** Консольный режим и GUI на Tkinter.

## 🛠 Технологический стек

* **Язык:** Python
* **ML Фреймворк:** PyTorch
* **NLP:** NLTK (Natural Language Toolkit)
* **GUI:** Tkinter
* **Данные:** JSON

## 📂 Структура проекта

* `train.py` — Скрипт для обучения модели. Загружает `intents.json`, обрабатывает данные и сохраняет обученную модель в `data.pth`.
* `chat.py` — Основной скрипт для запуска чат-бота в консольном режиме. Загружает модель и ведет диалог.
* `model.py` — Архитектура нейронной сети (`NeuralNet`).
* `nltk_utils.py` — Вспомогательные функции для обработки текста (токенизация, стемминг).
* `intents.json` — База знаний бота (намерения, фразы-триггеры, ответы).
* `app.py` / `ui.py` — Графический интерфейс пользователя (GUI).

## 🚀 Установка и Запуск

### 1. Установка зависимостей

Убедитесь, что у вас установлен Python. Затем установите необходимые библиотеки:

```bash
pip install torch torchvision nltk
```
Также необходимо загрузить пакет токенизации NLTK (обычно скрипт делает это автоматически, но можно выполнить вручную):

```Python
import nltk
nltk.download('punkt')
```
### 2. Настройка данных (опционально)
Откройте файл intents.json и добавьте свои намерения (intents). Структура:

tag: Категория (например, "greeting").

patterns: Что пишет пользователь ("Привіт", "Хай").

responses: Как отвечает бот ("Привіт!", "Доброго дня!").

### 3. Обучение модели
Перед первым запуском или после изменения intents.json необходимо обучить модель:

```Bash
python train.py
```
После завершения обучения будет создан файл data.pth.

### 4. Запуск чат-бота
Вариант А: Консольный режим

```Bash
python chat.py
```
Бот будет отвечать прямо в терминале. Для выхода введите quit.

Вариант Б: Графический интерфейс (GUI)

```Bash
python app.py
```
Запустится окно с чатом.

## 🤖 Пример работы

Модель классифицирует ввод пользователя и выбирает ответ с наибольшей вероятностью. Если уверенность модели низкая (prob < 0.75), бот ответит "Я не розумію вас".

> **User:** "Привіт"
> **Bot:** "Доброго дня!"
>
> **User:** "Як скласти резюме?"
> **Bot:** "Зазвичай, роботу можна знайти на спеціалізованих сайтах..."

## 👤 Автор

**borrusik**
